# Configuración de Producción para Ngrok
#spring.profiles.active=prod

# Base de datos - Configurar con variables de entorno
spring.datasource.url=jdbc:postgresql://localhost:5432/ecommerce_gt
spring.datasource.username=postgres
spring.datasource.password=51941405
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration
server.port=${PORT:8080}
server.servlet.context-path=/

# CORS Configuration para Netlify
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://tu-app-netlify.netlify.app}
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# JWT Configuration
jwt.secret=${JWT_SECRET:ecommercegt-super-secret-key-change-in-production-2025-muy-segura-para-desarrollo}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Email Configuration (opcional para producción)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:5MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:10MB}

# Logging Configuration
logging.level.com.ecommercegt=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=ERROR

# External File Storage (para producción usar cloud storage)
file.upload-dir=${UPLOAD_DIR:./uploads/productos}